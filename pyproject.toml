[project]
name = "tmdb-data-fetch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.32",
]

[project.scripts]
tmdbdf = "tmdbdf.app:main"
